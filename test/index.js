import { Session, XboxLive } from "../dist/index.js";

/**
new Session({
	hostName: "Tailvile.xyz",
	worldName: "Fun Fact: Tailvile is almost 2 years old.",
	version: "1.19.10",
	protocol: 534,
	players: 0,
	maxPlayers: 20,
	ip: "tailvile.xyz",
	port: 19132,
	log: true,
	connectionType: 6,
	keepVersionAndProtocolConstant: true,
	autoFriending: true,
});
*/
//console.log(new XboxLive(token).getAchievements());

// get a value from a promise as a non promise
